---
title: "Notes — T-001 (Bootstrap)"
type: "Executor Notes"
purpose: "Registro de desvios e decisões pontuais durante a execução de T-001"
---

# T-001 — Notes do Executor

> **Task:** T-001 — Bootstrap do repositório Next.js 15
> **Stack adapter:** TypeScript / Vitest / Playwright / Next.js
> **Data de execução:** 2026-04-29

---

## Desvios em relação ao backlog (registrados conforme regra Diferença #5 do core)

### Desvio 1 — Diretório `D:\companion-app` foi criado pelo Executor

O backlog T-001 assume que o repositório `companion-app` já existe (vazio exceto por `.git/`). Na execução real, o diretório não existia. Como o owner instruiu "(ou onde quer que esteja clonado o repo companion-app, vazio exceto por .git/)" e a regra Legion `Não pedir pro usuário executar passos` autoriza o Executor a contornar precondições triviais, o Executor:

1. Criou `D:\companion-app/`.
2. Rodou `git init -b main`.
3. Configurou `user.email` e `user.name` locais ao repo.

Nenhum remote foi configurado — fica para tarefa de hosting (fora do MVP backlog).

### Desvio 2 — Arquivo `apps/web/src/lib/version.ts` adicionado fora da lista declarada

O backlog T-001 declara como `code:`:
- `apps/web/package.json`
- `apps/web/tsconfig.json`
- `apps/web/next.config.ts`
- `apps/web/vitest.config.ts`
- `apps/web/src/app/page.tsx`

A instrução de execução do owner adicionou: "(exceto `src/lib/version.ts` se necessário pra teste — registrar em `notes.md` de T-001 se criar)". O Executor criou:

- `apps/web/src/lib/version.ts` — exporta função `getVersion(): string` retornando a versão semver lida de `package.json`.

Razão: o smoke test importa `@/lib/version` para ter algo concreto a verificar, e esse import é o que torna a fase RED reproduzível (test falha porque módulo não existe + porque `package.json` ainda não tem a entrada nem foi instalado).

### Desvio 3 — Pasta `notes/` criada na raiz do repo

Para manter este arquivo em um lugar previsível e separado do app, foi criada `notes/` na raiz de `companion-app`. Convenção sugerida para próximas tasks: `notes/T-XXX.md` apenas quando houver desvios reais.

### Desvio 4 — Infraestrutura implícita do Next.js App Router e do toolchain

Os arquivos abaixo não estão na lista `code:` do backlog T-001 mas são exigidos estruturalmente por peças que SÃO declaradas:

- **`apps/web/src/app/layout.tsx`** — Next.js App Router exige `layout.tsx` na raiz do diretório `app/` para qualquer rota funcionar. Sem ele, `pnpm dev` falha imediatamente. Conteúdo mínimo (HTML scaffold + `<html lang="pt-BR">`).
- **`apps/web/.eslintrc.json`** — Sem ele, `pnpm lint` (script declarado em `package.json`) falha. Conteúdo mínimo (`{ "extends": "next/core-web-vitals" }`).
- **`apps/web/.gitignore`** (criado em Desvio 1, listado no commit RED). Acompanha repo init.

Nenhum outro arquivo será criado. `postcss.config.*`, `tailwind.config.*` e `globals.css` ficam para a primeira task de UI que realmente usar Tailwind.

---

## Decisões de implementação (não desvios — escolhas dentro do espaço de design)

- **Package manager:** `pnpm` 10.29.3 (já instalado globalmente; respeita Sentinel L1 — sem `npm install -g`).
- **Tailwind 4:** instalado como dependência de dev mas **não habilitado em `app/globals.css`** nesta task (T-001 é bootstrap; configurar diretivas Tailwind sem componente que use é YAGNI). Próxima task de UI (T-008 ou T-010) ativa.
- **`pnpm-workspace.yaml`:** não criado nesta task. A estrutura `apps/web/` está pronta para virar workspace mas só formaliza quando uma segunda app/package aparecer (estrutura preparada, formalização adiada).
- **`src/app/globals.css`:** criado mínimo (apenas reset/`@tailwind` se aplicável; nesta task, vazio com comentário). Mantido para Next.js App Router não reclamar.
- **ESLint:** configuração default do `create-next-app` equivalente, gerada manualmente em `.eslintrc.json` minimal (`{ "extends": "next/core-web-vitals" }`).

---

## Verificações executadas antes do commit GREEN

- `pnpm typecheck` — esperado 0 errors.
- `pnpm lint` — esperado 0 errors.
- `pnpm test` — esperado 1 spec passando (smoke).
- `pnpm dev` em background + `curl http://localhost:3000/` retornando 200 — verificação do critério de aceite.
- `pnpm dev` startup time medido — esperado <10s.